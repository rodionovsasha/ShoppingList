language: java

jdk: oraclejdk8

sudo: false

install: true

script: mvn clean install -DskipDocker -Dwdm.chromeDriverVersion=2.36

branches:
  only: master

cache:
  directories:
    - $HOME/.m2/repository/
    - $HOME/.m2/wrapper/

notifications:
  email: false